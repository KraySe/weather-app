test('CityInfo render', async () => {
    // Estándar AAA
    // Arrange (arreglar, preparar, organizar todo lo que necesitamos)

    // Action (ejecutamos el test)
    
    // Assert (evaluar , preguntar)

})